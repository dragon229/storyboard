# 分鏡稿生成 · Skill 地圖

科普／知識型 YouTube 頻道的分鏡稿生成流程。從語音稿產出分鏡稿、出圖、驗證。

詞彙定義見 [CONTEXT.md](./CONTEXT.md)。關鍵決策的理由見 [docs/adr/](./docs/adr/)。

安裝位置：`D:\Cowork\分鏡稿生成\.claude\skills\`（專案 skill，只在本目錄底下生效）

---

## 全貌

```
storyboard ──────────────────── ① 大 skill：語音稿 → 分鏡稿 → 圖
  ├─ storyboard-segment          切分         全局
  ├─ storyboard-intent           段落意圖判讀  逐格
  ├─ storyboard-depiction        呈現手法選擇  全局 ← 障壁
  ├─ storyboard-framing          取景設計      逐格
  ├─ storyboard-staging          場面調度      逐格
  ├─ storyboard-compose          組裝          逐格
  └─ storyboard-render ────────  ② 出圖（也可獨立呼叫）

storyboard-verify ───────────── ③ 驗證大 skill
  ├─ storyboard-verify-draft     乙：分鏡稿 vs 原文   出圖前
  ├─ storyboard-verify-image     甲：圖 vs 提示詞     出圖後
  └─ storyboard-verify-quality   C：品質準則審查      任意時機
```

**可自動觸發**：`storyboard`、`storyboard-render`、`storyboard-verify`
**其餘全部** `disable-model-invocation: true` — 只能由上層 skill 或使用者以 `/名稱` 呼叫。

---

## ① storyboard（大 skill）

**目標**：一份語音稿進去，一批圖出來。

**輸入**：語音稿檔（`.md` / `.txt`）＋ 系列名稱
**輸出**：`output/<系列>/`——**一支影片一個資料夾**，分鏡稿與圖片都放進去：

```
output/<系列>/
├── <系列>.json     分鏡稿
└── images/         s01.png … sNN.png
```

**執行順序**：

| # | 呼叫 | 粒度 | 說明 |
|---|------|------|------|
| 1 | `storyboard-segment` | 全局 | 切出分鏡格 |
| 2 | `storyboard-intent` | 逐格 | 每格判修辭功能＋情緒偏移 |
| 3 | `storyboard-depiction` | **全局** | **障壁**：必須等 2 全部完成 |
| 4 | `storyboard-framing` | 逐格 | |
| 5 | `storyboard-staging` | 逐格 | |
| 6 | `storyboard-compose` | 逐格 | |
| 7 | `storyboard-render` | 全局 | |

**中途不停**。跑完才交付。想檢視就跑 `storyboard-verify`。

**讀取的資產**（放專案目錄，不放 skill 目錄）：
- 風格規範（畫風文字、頻道基調、輸出尺寸）
- 主持角色資產（`input_character.png` ＋ 文字外觀描述）

---

## ② 六支子 skill

### storyboard-segment（切分）

**目標**：把語音稿切成一格一格。

- **輸入**：語音稿全文
- **輸出**：分鏡格清單（`id` / `source` 原文段落）
- **規則核心**：**一個「可畫的論述單元」⇔ 一格**
  - 不用字數、不用秒數
  - 一個論述單元內若還有第二個獨立可畫的東西 → 再切一格
  - 沒有第二個可畫的東西 → 維持一格，無論它多長
- **呼叫**：無

> ⚠ 舊專案的四條切分條件（地點改變／時間跳躍／視覺焦點轉移／情緒轉折）**全部不適用**——
> 那些依賴敘事，科普稿沒有地點與時間線。

---

### storyboard-intent（段落意圖判讀）

**目標**：讀一段話，說出它在論述裡幹嘛、溫度多少。

- **輸入**：單一分鏡格的 `source`（＋前後各一格當上下文）
- **輸出**：`修辭功能` ＋ `情緒偏移` ＋ `是否為推演`
- **性質**：逐格、局部、**讀出來的**、唯一正解
- **呼叫**：無

**修辭功能：9 項**（以「這段對觀眾的認知做了什麼」分類，全部是動詞）

| 功能 | 例子 |
|---|---|
| **拋出鉤子** | 「閃電的溫度是太陽表面的五倍。」／「為什麼閃電是彎的，不是一條直線？」 |
| **建立前提** | 「先講一件事：空氣其實不導電。」 |
| **點出直覺** | 「直覺上你會覺得，閃電是從天上打下來的。」 |
| **推翻** | 「但你看到的那道光，其實是從地面往上竄的。」 |
| **拆解機制** | 「雲底累積負電荷，地面被感應出正電荷…」 |
| **佐證** | 「1971 年有人用每秒兩萬張的高速攝影機拍下整個過程。」 |
| **推進** | 「所以如果空氣要被電離才導電，那電壓就必須高到某個門檻。」 |
| **敘事** | 花 40 秒講富蘭克林淋著雨、鑰匙冒火花、伸手去碰 |
| **收束** | 「這就是為什麼閃電是彎的——它不是被打下來的，是被摸索出來的。」 |

**判讀必須看上下文**：功能是內容**在論述裡所處位置**的屬性，不是內容本身的屬性。
同一則富蘭克林軼事，放開場＝拋出鉤子，放機制之後＝佐證，鋪成 40 秒＝敘事。

**功能是論述單元層級的屬性**：同一單元切出的多格填同一個值，不再細分子分類
（切分後每格 `source` 本來就不同，下游拿得到差異；再貼子標籤是換句話說）。

**情緒偏移**：相對於風格規範裡的頻道基調的偏離。多數格無偏移。

> 兩者合併在同一支判讀，而非分兩支——同一次閱讀就能得到，分開會互相打架。

---

### storyboard-depiction（呈現手法選擇）

**目標**：決定每一格用什麼方式畫，以及要走哪條 workflow。

- **輸入**：**整份**分鏡格清單（含每格的 `source` 與 `修辭功能`）
- **輸出**：每格的 `呈現手法` ＋ `workflow 類型`
- **性質**：全局、**選出來的**、多解並存
- **呼叫**：無

**呈現手法：8 項**（全部是名詞，講畫面怎麼組織）

| 手法 | 例子 |
|---|---|
| **實景** | 雷雨夜的空曠原野，遠處一道閃電 |
| **隱喻** | 講電流時畫水在管子裡流；講電位差時畫一座水壩 |
| **示意圖** | 雲底一排負號、地面一排正號、中間標出電位差 |
| **並置** | 左邊「以為的閃電」由上往下，右邊「真實的」由下往上 |
| **序列** | 一格裡由左到右畫出先導向下摸索的三個時刻 |
| **尺度對照** | 閃電通道旁邊放一支鉛筆當比例尺 |
| **主持人出鏡** | 主持角色一臉懷疑地看著避雷針 |
| **細部放大** | 鑰匙尖端冒出的那一小簇火花 |

> **沒有「數據視覺化」這一項，是刻意的。** 生圖模型畫不出正確圖表——軸標籤變假文字、
> 比例隨機。預設把數據轉譯成不需要精確文字的視覺（尺度對照、並置）；真的非圖表不可時，
> 標記「需後製」不生圖。而且轉譯本來就是更好的科普手法：長條圖要觀眾讀刻度，
> 尺度對照讓觀眾直接感覺到差距。

### 選擇流程：三層疊加

**第一層 — 傾向表**

| 修辭功能 | 首選 | 可用 | **避免** |
|---|---|---|---|
| 拋出鉤子 | 實景、細部放大、尺度對照 | 隱喻 | **示意圖** |
| 建立前提 | 示意圖、實景 | 隱喻 | 細部放大 |
| 點出直覺 | 隱喻、示意圖 | 實景 | — |
| 推翻 | 並置、示意圖 | 實景 | — |
| 拆解機制 | 示意圖、隱喻、序列 | — | **實景** |
| 佐證 | 實景、尺度對照 | 並置 | **隱喻** |
| 推進 | 示意圖、序列 | 隱喻 | 實景 |
| 敘事 | 實景、細部放大 | 主持人出鏡 | **示意圖** |
| 收束 | 主持人出鏡、實景、隱喻 | 並置 | 細部放大 |

「避免」欄是硬約束，理由都不直覺：
- **鉤子不用示意圖**：示意圖是拿來解釋的，開場就解釋，好奇心當場被殺掉
- **拆解機制不用實景**：機制通常看不見（電荷、分子、力），硬拍會拍到一個什麼都沒發生的畫面
- **佐證不用隱喻**：佐證說「這是真的」，隱喻說「這像什麼」；用隱喻佐證，可信度直接掉
- **敘事不用示意圖**：敘事要代入感，示意圖是抽離的

跨格規則：
- **「點出直覺 → 推翻」兩格的畫面必須構成對照**（最好同構圖同視角，只有內容相反）。
  單看任一格都對、兩格沒對上就白費。
- **收束回扣開場意象時衝擊力最強**，所以收束那格要看第一格用了什麼。

**第二層 — 四個加權問題**（強制逐題回答，據以修正第一層的傾向）

| 問題 | 影響 |
|---|---|
| 觀眾熟不熟悉這個東西？ | 完全陌生 → 幾乎一定要**隱喻**，不管功能是什麼 |
| 講的是實體還是關係？ | 關係／流程／因果 → 只能**示意圖**，實景畫不出「因為所以」 |
| 有沒有現成的真實案例？ | 有 → **實景**的說服力遠勝其他 |
| 前兩格用了什麼？ | 連續重複 → 強制換手法，即使表上是首選 |

> 這一層是**強制步驟**，不是可選的靈感。範例：「電流就是電子在導線裡漂移，慢得像蝸牛」
> ——功能＝拆解機制，表首選示意圖，直接照做會畫出「一條導線裡一排小球」，觀眾看完還是
> 不懂。問完第一題（完全陌生）才會修正成隱喻：一條擠滿人的走廊，每人只挪一小步，
> 但擠壓瞬間傳到另一端。**兩張圖的差距完全來自有沒有被強迫問那一題。**

**第三層 — 節奏規則**
- 同一手法**不連續超過 2 格**
- 主持角色**每 10 格至少出現一次**（暫定值，跑過幾支影片後再調）
- 示意圖與實景交錯，避免視覺疲勞

> 三層各自對應一種失敗：表沒寫好 → 系統性選錯；問題沒問 → 個別格不合適；
> 節奏沒管 → 整支影片單調。分開才知道該修哪裡。

**決定 workflow**：
| 呈現手法 | workflow |
|---|---|
| 主持人出鏡 | 文生圖 ＋ 角色參考圖 |
| 其餘 | 純文生圖 |

> 這是整條流程唯一的**障壁**——必須等所有格的意圖判讀完成才能開始。

---

### storyboard-framing（取景設計）

**目標**：決定這一格怎麼取景。

- **輸入**：`修辭功能` ＋ `呈現手法` ＋ `source`
- **輸出**：`景別` / `視角` / `構圖` / `光線天氣`（＋圖解類的版面層）
- **性質**：逐格、查表
- **呼叫**：無

**以呈現手法為主軸決定用哪套詞彙**，修辭功能只做強度修正。

| 呈現手法 | 用哪套詞彙 |
|---|---|
| 實景、主持人出鏡、細部放大 | **實景詞彙** |
| 示意圖、並置、序列 | **圖解詞彙** |
| 隱喻、尺度對照 | **兩套都要**（畫面是實景的，但要讀得出對應關係） |

**實景詞彙**：景別 / 視角 / 構圖 / 光線天氣

**圖解詞彙**：
- **版面**：置中單體 / 左右分欄 / 上下堆疊 / 環形 / 由左到右的流程帶
- **資訊層次**：主體佔多大、次要元素怎麼退後（虛化、降飽和、縮小）
- **方向性**：箭頭或動勢往哪走（時間流向、因果流向）
- **對比配置**：對稱（平等比較）還是失衡（一方壓倒另一方）

生成出來的圖解仍是一張插畫，仍有構圖與視角——所以圖解詞彙是**加一層**，不是換一套。

### 全域禁止文字

生圖模型寫出來的文字幾乎都是亂碼——形狀像字但不是字。一張標了六個假詞的示意圖比沒標籤更糟：
它看起來像出錯，觀眾會分心去讀那些字。

**預設：畫面完全不出現文字**，資訊全靠視覺編碼承載——顏色（正電紅／負電藍）、形狀
（實心／空心）、箭頭方向、粗細、明暗、密度。

> 這條限制會逼出更好的圖。不能寫「這是負電荷」，就得讓畫面**自己說明**——用一整片藍色
> 密集小點，觀眾不用讀字就知道那裡「有很多某種東西」。比一個標著假字的箭頭有效得多。

**逃生口**：真的需要專有名詞（人名、年份、學術詞）時，該格標記「需後製文字」，
prompt 明確留出空白區域放字。

> 組裝 skill 的全域禁止項因此要加 `no text, no labels, no letters, no numbers`，
> **每一格都加**。強度比舊專案的 `no subtitles` 高得多。

> 舊專案 `語音稿轉提示詞規範.md` 的「七大焦點類型 × 取景組合」對照表（景別/視角/構圖
> 三維度的完整定義與感受對應）是實景詞彙的主要素材來源，需依科普情境改寫。

---

### storyboard-staging（場面調度）

**目標**：寫出畫框裡實際有什麼。

- **輸入**：`呈現手法` ＋ `景別` ＋ `source`
- **輸出**：畫框內容的英文描述
- **性質**：逐格、展開
- **呼叫**：無

**核心約束：受景別限制**。局部特寫的格不可同時描述全身姿態——兩者矛盾時模型退回中景，
特寫意圖失效。所以順序必然是「先定取景，再寫內容」。

**反向約束（實測）**：場面調度寫錯了會**反殺景別**。
最典型的是——**prompt 只要提到鞋子，模型就必須畫全身**，寫好的景別直接作廢。
因此主持角色的服裝描述拆成 upper / mid / lower 三段，依景別決定用到哪一段：

| 景別 | 用到 |
|---|---|
| 特寫、近景 | upper |
| 中景 | upper + mid |
| 全身、遠景 | upper + mid + lower |

（同時：costume 必須整句用上，只寫上衣模型會自己配色，角色變成全身同色。）

---

### storyboard-compose（組裝）

**目標**：把上面全部拼成一段可送 ComfyUI 的提示詞。

- **輸入**：場面調度 ＋ 取景 ＋ 情緒語句 ＋ 風格規範
- **輸出**：`prompt`
- **性質**：薄，但不是純字串拼接
- **呼叫**：無（讀風格規範資料檔）

**殘留的條件判斷**（這就是它該是 skill 而非拼接函式的原因）：
- **每一格** → 加 `no text, no labels, no letters, no numbers`（全域禁止文字）
- 全域 → 加 `no letterboxing, no black bars, full bleed 16:9 frame`
- 畫面無人物 → 加 `no people visible`
- `是否為推演 = true` → 套用風格規範裡的**推演調性**子句
- 標記「需後製文字」的格 → prompt 明確留出空白區域
- 最後跑一次自檢清單

---

## ③ storyboard-render（出圖）

**目標**：把分鏡稿變成圖。

- **輸入**：分鏡稿 JSON
- **輸出**：圖片
- **呼叫**：無（呼叫 ComfyUI HTTP API）

獨立成 skill 的理由：它的**失敗性質**跟前面完全不同。前面壞是「規範沒寫清楚」，
這裡壞是「ComfyUI 沒開／模型沒載／seed 不好」。混在一起會分不清哪層壞了。

也讓你能「只改幾格的 prompt，只重出那幾格」。

**已實測跑通**（2026-08-03）：兩條 workflow 都建在 `comfyUI_workflow/`，共用
`flux-2-klein-base-4b`，參考圖經 `ReferenceLatent` 掛進正向條件。約 40–50 秒／張。
執行腳本 `scripts/render.py`。

三條實測結論見該 skill 檔：文字產不出一致角色（角色格必走參考圖路徑）、
參考圖素底會滲入（背景要寫具體）、構圖詞控制力弱。

---

## ④ storyboard-verify（驗證大 skill）

**目標**：統整所有驗證手段。未來有新的驗證方式就掛在這底下。

- **輸入**：要驗什麼（分鏡稿？圖？）
- **輸出**：問題清單
- **呼叫**：底下三支

**只報告，不自動改。** 你看完再決定要不要修。

---

### storyboard-verify-draft（乙：分鏡稿 vs 原文）

**目標**：在燒圖之前，攔截判讀錯誤。

- **輸入**：語音稿原文 ＋ 分鏡稿
- **輸出**：兩份判讀有差異的格
- **時機**：出圖**前**

**作法：遮蔽式反向獨立判讀。**

1. **只給它原文**，不給它現有的判讀結果
2. 讓它獨立判一次修辭功能／呈現手法／取景
3. 比對兩份，**差異處就是可疑點**

> 為什麼不能照同一套規範重跑：同一套規則跑第二次只抓得到疏漏，抓不到系統性偏誤。
> 而系統性偏誤（規範定義寫太寬，一半段落都被判成同一類）正是最需要抓的。
> **遮蔽帶來獨立性，獨立性才有驗證力。**

這也是你選「中途不停」之後的主要檢視機制——你不用掃 30 格，只看被標記的那幾格。

---

### storyboard-verify-image（甲：圖 vs 提示詞）

**目標**：模型有沒有畫出我叫它畫的東西。

- **輸入**：圖片 ＋ 該格 prompt
- **輸出**：不符合的項目清單
- **時機**：出圖**後**

**作法：圖與 prompt 一起丟，直接問「哪些項目不符合」。** 不預先產 claims 清單。

> 不由生成端產 claims 的理由：那是自己出考題給自己考——claims 跟 prompt 是同一次思考的
> 產物，生成時漏掉的東西 claims 也會一起漏掉，兩邊同時錯就會驗證通過。

**已知弱點與對策**：這種問法對「多了不該有的東西」很強，對「少了該有的東西」較弱
——缺席的東西不會主動跳出來喊。對策是 skill 內部要求它**先把 prompt 要求的元素逐項
過一遍再看圖**，而不是憑印象挑毛病。

---

### storyboard-verify-quality（C：品質準則審查）

**目標**：用獨立於生成規範的準則審一遍。

- **輸入**：分鏡稿（或圖）
- **輸出**：違反準則的格

**第一批準則**（從舊專案踩過的坑萃取，與生成規範獨立）：

| 準則 | 問題 |
|---|---|
| 冗餘檢查 | 畫面是不是只把旁白「渲染成看得見的字」？畫面該給旁白**沒說**的東西 |
| 單一焦點 | 有沒有把兩件事塞進同一張？ |
| 數量的視覺轉換 | 旁白說「很多／密密麻麻」時，有沒有真的畫一大堆？（越密個體越模糊，規模感反而下降；正解是選 3–5 個有差異的代表，靠情緒語句傳達規模） |
| 具體可畫 | 有沒有拿抽象名詞當畫面主體？ |

其餘準則待跑過幾支影片、看到反覆出現的問題後再補。

---

## 編輯器（不是 skill，是工具）

`啟動編輯器.bat` → http://127.0.0.1:8420

給人用的手動修改介面，跟 skill 樹平行——skill 負責批次產出，編輯器負責逐格微調。
兩邊讀寫同一份 `output/<系列>/<系列>.json`。

| | |
|---|---|
| 左欄 | 語音稿逐句，拖曳選範圍；已分配的句子灰掉並標分鏡編號；單擊跳到該分鏡 |
| 右欄 | 預覽圖／候選圖、`source`（可重新選取台詞）、判讀欄位、取景 8 個下拉、`staging`、`prompt` |
| 自動組裝 | 改下拉或 `staging` → prompt 依 `style.json` 立刻重算 |
| 手改保護 | 一旦手動編輯 prompt 就標「已手改」，之後不再自動覆蓋；按「重新組裝」還原 |
| 出圖 | 單張生成、批次 8 張候選 → 點縮圖挑一張 |
| 重新編號 | 依台詞順序重排，JSON 與圖片一起改名 |

**組裝邏輯集中在 `scripts/compose.py`**，編輯器與批次流程共用同一份，避免兩邊各寫一套而漂掉。

---

## 明確排除在這棵樹之外的東西

| 東西 | 為什麼不在 | 在哪 |
|---|---|---|
| **主持角色參考圖的產製** | 節奏不同——這棵樹每支影片跑一次，角色圖整個頻道做一次 | 你自己跑 `01/02-Character-Sheet` workflow，產物放專案目錄 |
| **風格規範** | 沒有判斷邏輯，是資料不是工具 | 專案目錄的資料檔，由 compose 讀取 |
| **世界觀／環境／供電／喪屍分型／破壞素材** | 那是舊劇情動畫專案的設定資產，科普頻道不需要 | 已廢棄 |
| **多角色一致性** | 本頻道只有一位主持角色，是布林值不是規則集 | 不需要 |
| **自動重生迴圈** | 過度工程。格數少、圖單純，且你本來就要人工看 | 需要時再加，可掛在 verify 底下 |
